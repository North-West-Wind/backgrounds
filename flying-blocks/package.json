{
  "name": "flying-blocks-background",
  "version": "1.0.0",
  "description": "Flying blocks background for streaming.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r dist/flying-blocks ../public/ && cp dist/index.html ../public/flying-blocks/ && cp -r resources ../public/flying-blocks/resources",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.154.0",
    "vite": "^3.0.4"
  },
  "dependencies": {
    "three": "^0.128.0"
  }
}
